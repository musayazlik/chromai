{
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm exec prisma migrate deploy && pnpm exec prisma generate && pnpm run build",
  "env": {
    "NEXT_PUBLIC_SITE_URL": ""
  }
}
